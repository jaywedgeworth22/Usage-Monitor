# 2026-08-10 — Default theme is light

## Changes
- Web ThemeProvider `defaultTheme="light"` (was system)
- iOS AppSettings default `.light` (was `.system`)
- Fleet agent rule mirrored in AGENTS.md

ASC screenshots already light — no replace.

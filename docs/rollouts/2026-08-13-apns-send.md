# Usage Monitor APNs send

HTTP/2 sender for the same budget and provider alert pages that already go to Pushover.

## What shipped

- `src/lib/apns.ts` — ES256 provider JWT, `node:http2` transport, injectable mock for tests.
- `alert-delivery` grows an `apns` channel when `APNS_*` is configured and fans out to `ApnsDeviceToken`.
- Dead tokens (410 / 400 BadDeviceToken) are deactivated.  Auth failures do not retry forever.
- Tests never open a socket to Apple.

## Secrets (Infisical `usage-monitor` prod)

| Name | Purpose |
| --- | --- |
| `APNS_KEY_ID` | 10-char key id |
| `APNS_TEAM_ID` | `CC8UTF7ATG` |
| `APNS_BUNDLE_ID` | `services.jays.usage.client.monitor` |
| `APNS_P8` | raw PEM or base64 of the .p8 |
| `APNS_PRIVATE_KEY_B64` | optional alias |

No UM-named `.p8` is in `~/.secrets`.  `AuthKey_N3949G7CN6.p8` is the ASC API key, not APNs.  The sender is ready; production cannot actually deliver until those four keys exist in Infisical.  Verify writes by presence + length only.

## Owner follow-up

Enable the Push Notifications capability on the Usage Monitor App ID if it is still off.  Then add the four keys and allow notifications once on a TestFlight device.

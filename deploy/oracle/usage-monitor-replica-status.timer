[Unit]
Description=Periodically refresh the Litestream replica heartbeat for /api/ready

[Timer]
# Ten-minute cadence keeps well inside the one-hour
# LITESTREAM_REPLICA_MAX_AGE_SECONDS staleness budget, so several consecutive
# probe failures are needed before strict readiness flips - a single slow
# Garage listing never pages anyone.
OnBootSec=2min
OnUnitInactiveSec=10min
RandomizedDelaySec=30s
AccuracySec=10s
Persistent=true
Unit=usage-monitor-replica-status.service

[Install]
WantedBy=timers.target
